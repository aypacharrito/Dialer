PACIFICA WORKSPACE RELEASE UPDATE

Cumulative update for GitHub main 748efc4. Includes the previous Workspace
Polish Update; you only need to apply this package.

INSTALL
Extract the ZIP. Upload the extracted app and tests folders into the repository
root, merging folders and replacing matching files. Include all new files.
Commit using your normal GitHub upload workflow to trigger deployment.
Do not add the unopened ZIP as source code.

WHAT CHANGED
- Compact dialer, connected-call details, responsive header/sidebar, motion,
  unified text scaling, and cross-screen layout fixes from the previous update.
- Active call bar in other CRM views with open dialer, mute, hold/resume,
  pause queue, and end/cancel controls, using the existing call functions.
- Lead details use aligned rows instead of individual rounded boxes.
- Placeholder imported fields are hidden without changing stored records;
  valid zero values are preserved.
- Incoming-call and phone-settings dialogs now contain keyboard focus.
- Narrow-screen inbox/thread navigation and keyboard channel switching.
- Email settings show checking, connected, setup-needed and error states,
  with retry handling; technical configuration is expandable.
- Workspace number settings have expandable details and copy feedback.
- Contact drawer Call loads the selected contact into the dialer.

VERIFICATION
Production build and TypeScript: passed with locked dependencies.
77 existing domain tests: passed (calling routes, imports, messaging,
queue/lead logic, post-call handling, automation, workspace settings).
3 new lead-presentation tests: passed.
3 new DOM interaction tests: passed (active-call actions, email retry,
clipboard success/failure). Prior dialog/inbox tests also passed in this session.
Targeted ESLint: passed for new/changed standalone components and helpers.
CRMClient retains two pre-existing react-hooks/purity Date.now lint findings.
Local Node version: 24; repository deployment target: 22.

REMAINING RELEASE CHECKS
The environment blocked browser preview. No visual verification was possible.
Live calling, recording, real email/SMS delivery, and cross-device sync were
not exercised against production services. Review the deployment at narrow
and wide window sizes, in both themes and each display-size setting. Make a
controlled call, navigate away during it, return to the dialer, and save the
result. Test an inbound call, a message, and an email with consenting test
recipients. These are verification requirements, not claims of completion.
