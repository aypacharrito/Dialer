PACIFICA — RESIZE & MOTION FOLLOW-UP

This follows the compact dialer update you already uploaded.
Base: aypacharrito/Dialer main, commit 89d3ecf.

INSTALL
1. Extract this ZIP.
2. Open your Dialer repository on GitHub, on main.
3. At the repository root, choose Add file > Upload files.
4. Drag in the extracted app folder. Preserve its folder structure.
5. Commit and let Vercel finish deploying, then reload Pacifica.

Only app/CRMClient.tsx and app/product-system.css are replaced.
The previous compact dialer and font changes remain included in these files.

CHANGES
- The header wraps its controls naturally instead of reserving oversized fixed columns.
- The queue selector stays in the header flow, preventing overlap with other controls.
- Sidebar collapses to a 72px rail at 1280px, then 60px on narrow phones.
- Navigation has hover labels and an accessible current-page indicator.
- Settings stays reachable on the collapsed sidebar.
- Header actions wrap into separate rows when needed; no action is hidden to force a fit.
- The keypad, call controls and lead details stack based on available workspace width.
- Long contact details and integration URLs can wrap.
- Wide tables scroll within their own areas.
- Panels enter with brief 180ms transitions; controls have subtle hover/press feedback.
- Reduced-motion preferences disable the new animations.

VALIDATION
Production Next.js build passed, including TypeScript and all 37 static page generation steps.
Source diff passed whitespace/error checks.
Browser visual verification was blocked by ERR_BLOCKED_BY_CLIENT when opening the local preview. This package has not been visually verified or used for a live call.

AFTER DEPLOYMENT
Check full width, a restored window around 1276px, and a narrow window.
Confirm header controls do not overlap, Settings remains reachable, and Keypad opens/closes.
Check Settings > Workspace > Display size with Extra large and check both themes.
Live calling behavior was not changed by this follow-up.
