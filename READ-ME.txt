PACIFICA WORKSPACE POLISH UPDATE

Built against GitHub main 748efc4 (your latest upload).

INSTALL
Extract this ZIP. Upload the extracted app folder into the repository root,
merging it with the existing app folder and replacing the matching files.
Include both new files in app/hooks and app/lib. Do not upload the ZIP itself
as source code. Commit the changes to trigger your normal deployment.

CHANGES
- Retains the compact dialer, connected-call details, responsive header/sidebar,
  and subtle motion from your previous upload.
- Unifies display-size scaling across legacy and current UI styles.
- Refines spacing, surfaces, forms, metrics, settings, and small-window layouts.
- Adds an inbox/thread navigation flow for narrow message windows.
- Adds keyboard channel switching, dialog focus containment/restoration,
  scroll locking, visible focus, and accessible field labels.
- Fixes the contact drawer Call action to load the selected contact.

VERIFICATION
- Production build and TypeScript completed successfully using locked dependencies.
- 27 interaction and domain tests passed (dialogs, messages, lead priority,
  post-call behavior, and workspace profiles).
- Targeted ESLint passed for the new focus helper/hook and changed MessagesCenter
  and PostCallDispositionModal components.
- Full CRM lint still has two pre-existing Date.now purity findings.
- Browser preview was blocked by the environment security policy; there was no
  visual verification. Live calling and external service delivery were not tested.
- Local runtime was Node 24; the repository targets Node 22 on deployment.

DEPLOYED REVIEW
Check narrow and wide windows, all display-size settings, dark/light themes,
contact Call selection, inbox navigation, dialog keyboard behavior, and one
controlled live call through answer and saved disposition.
